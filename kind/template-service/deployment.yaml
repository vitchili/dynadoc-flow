apiVersion: v1
kind: Secret
metadata:
  name: template-service-secret
  namespace: template-service
type: Opaque
data:
  db-password: cm9vdA== # root
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: template-service-config
  namespace: template-service
data:
  APP_NAME: "Template Service"
  APP_ENV: "production"
  APP_KEY: "base64:your-app-key-here"
  APP_DEBUG: "false"
  APP_URL: "http://template-service"

  LOG_CHANNEL: "stack"
  LOG_DEPRECATIONS_CHANNEL: "null"
  LOG_LEVEL: "debug"

  DB_CONNECTION: "mysql"
  DB_HOST: "mysql-service.shared-infrastructure.svc.cluster.local"
  DB_PORT: "3306"
  DB_DATABASE: "template_service"
  DB_USERNAME: "root"

  REDIS_HOST: "redis-service.shared-infrastructure.svc.cluster.local"
  REDIS_PASSWORD: "null"
  REDIS_PORT: "6379"

  KAFKA_BROKERS: "kafka-service.shared-infrastructure.svc.cluster.local:9092"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: template-service-storage
  namespace: template-service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: template-service
  namespace: template-service
  labels:
    app: template-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: template-service
  template:
    metadata:
      labels:
        app: template-service
    spec:
      containers:
        - name: template-service
          image: template-service:latest
          ports:
            - containerPort: 9000
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: template-service-secret
                  key: db-password
          envFrom:
            - configMapRef:
                name: template-service-config
          volumeMounts:
            - name: template-service-storage
              mountPath: /var/www/template
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            exec:
              command:
                - php-fpm
                - -t
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - php-fpm
                - -t
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: template-service-storage
          persistentVolumeClaim:
            claimName: template-service-storage
---
apiVersion: v1
kind: Service
metadata:
  name: template-service
  namespace: template-service
  labels:
    app: template-service
spec:
  selector:
    app: template-service
  ports:
    - port: 9000
      targetPort: 9000
      name: php-fpm
  type: ClusterIP
